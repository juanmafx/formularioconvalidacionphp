
<p align="center"><strong>Formulario PHP con Validación y Envío a Página Objetivo</strong></p>

<p align="center">
Un formulario simple en PHP con validación de campos y envío de datos.
</p>

<p align="center">
<img src="https://img.shields.io/badge/PHP-5.6%2B-blue.svg" alt="PHP Version">
<img src="https://img.shields.io/badge/Estado-Activo-brightgreen.svg" alt="Estado del Proyecto">
<img src="https://img.shields.io/badge/Licencia-Libre-lightgrey.svg" alt="Licencia">
</p>

## Acerca del Formulario

Este proyecto proporciona un formulario que valida datos esenciales antes de su envío. Es ideal como base para proyectos que requieran captura de información del usuario.

El formulario valida los siguientes campos obligatorios:

- **Nombre**
- **Email**
- **Teléfono**

Gracias a HTML5, el campo **Email** aplica una validación automática de formato, mientras que **Nombre** y **Teléfono** son validados manualmente mediante PHP.

## Funcionamiento

Este formulario funciona con dos archivos principales:

- `class.phpmailer.php`
- `index.php`

En `index.php`, antes de la etiqueta `<html>`, se incluye el código PHP necesario para procesar la validación y el envío de los datos.

El formulario HTML contiene los `input` para capturar los datos requeridos.

## Requisitos

- Servidor web con PHP 5.6 o superior
- Navegador web moderno con soporte para HTML5

## Instalación

1. Clonar o descargar los archivos del proyecto.
2. Asegurarse de tener habilitada la función de envío de correos en el servidor.
3. Configurar `class.phpmailer.php` si se requiere personalización en el envío de correos.
4. Ejecutar `index.php` en un entorno de servidor local o remoto.

## Palabras clave

- Formulario PHP
- Validación de formulario
- Formulario de contacto
- Formulario de contacto con validación
- Envío de correos en PHP
- PHP Mailer
- Validación HTML5

## Contribuciones

Gracias por considerar contribuir al proyecto. ¡Cualquier mejora, comentario o sugerencia es bienvenida!

## Código de Conducta

Para mantener una comunidad abierta y respetuosa, por favor revisa y respeta las buenas prácticas de contribución.

## Vulnerabilidades de Seguridad

Si descubres alguna vulnerabilidad de seguridad en este formulario, por favor contáctame para poder solucionarlo de inmediato.

## Licencia

Este proyecto es software libre y está disponible bajo una licencia abierta. Puedes usarlo, modificarlo y adaptarlo libremente.

---
https://github.com/juanmafx


## 🇫🇷 Français

**Formulaire simple en PHP avec validation des champs et envoi des données.**

### À propos du formulaire

Ce projet fournit un formulaire qui valide les données essentielles avant leur envoi. Il est idéal comme base pour les projets nécessitant la collecte d'informations utilisateur.

Le formulaire valide les champs suivants comme obligatoires :

- **Nom**
- **Email**
- **Téléphone**

Grâce à HTML5, le champ **Email** applique une validation automatique du format, tandis que **Nom** et **Téléphone** sont validés manuellement via PHP.

### Fonctionnement

Ce formulaire fonctionne avec deux fichiers principaux :

- `class.phpmailer.php`
- `index.php`

Dans `index.php`, avant la balise `<html>`, le code PHP nécessaire pour traiter la validation et l'envoi des données est inclus.

Le formulaire HTML contient les `input` pour capturer les données requises.

---

## 🇬🇧 English

**Simple PHP form with field validation and data submission.**

### About the Form

This project provides a form that validates essential data before submission. It is ideal as a foundation for projects that require user information collection.

The form validates the following required fields:

- **Name**
- **Email**
- **Phone**

Thanks to HTML5, the **Email** field applies automatic format validation, while **Name** and **Phone** are manually validated using PHP.

### How It Works

This form works with two main files:

- `class.phpmailer.php`
- `index.php`

In `index.php`, before the `<html>` tag, the PHP code required to process the validation and submission of the data is included.

The HTML form contains `input` fields to capture the required data.

---

## 🇩🇪 Deutsch

**Einfaches PHP-Formular mit Feldvalidierung und Datenübertragung.**

### Über das Formular

Dieses Projekt bietet ein Formular, das wichtige Daten vor der Übertragung validiert. Es ist ideal als Grundlage für Projekte, die die Erfassung von Benutzerinformationen erfordern.

Das Formular validiert die folgenden erforderlichen Felder:

- **Name**
- **E-Mail**
- **Telefon**

Dank HTML5 wird das **E-Mail**-Feld automatisch auf das Format validiert, während **Name** und **Telefon** manuell mit PHP validiert werden.

### Funktionsweise

Dieses Formular funktioniert mit zwei Hauptdateien:

- `class.phpmailer.php`
- `index.php`

In `index.php` wird vor dem `<html>`-Tag der erforderliche PHP-Code zur Verarbeitung der Validierung und Übertragung der Daten eingefügt.

Das HTML-Formular enthält `input`-Felder zur Erfassung der erforderlichen Daten.

---

## 🇮🇹 Italiano

**Modulo semplice PHP con validazione dei campi e invio dei dati.**

### Informazioni sul modulo

Questo progetto fornisce un modulo che valida i dati essenziali prima dell'invio. È ideale come base per i progetti che richiedono la raccolta di informazioni dagli utenti.

Il modulo convalida i seguenti campi obbligatori:

- **Nome**
- **Email**
- **Telefono**

Grazie a HTML5, il campo **Email** applica una validazione automatica del formato, mentre **Nome** e **Telefono** vengono convalidati manualmente tramite PHP.

### Come funziona

Questo modulo funziona con due file principali:

- `class.phpmailer.php`
- `index.php`

In `index.php`, prima del tag `<html>`, viene incluso il codice PHP necessario per elaborare la convalida e l'invio dei dati.

Il modulo HTML contiene campi `input` per raccogliere i dati richiesti.

---

## 🇵🇹 Português

**Formulário simples em PHP com validação de campos e envio de dados.**

### Sobre o Formulário

Este projeto fornece um formulário que valida dados essenciais antes de seu envio. É ideal como base para projetos que exigem a coleta de informações do usuário.

O formulário valida os seguintes campos obrigatórios:

- **Nome**
- **Email**
- **Telefone**

Graças ao HTML5, o campo **Email** aplica uma validação automática de formato, enquanto **Nome** e **Telefone** são validados manualmente via PHP.

### Como Funciona

Este formulário funciona com dois arquivos principais:

- `class.phpmailer.php`
- `index.php`

No arquivo `index.php`, antes da tag `<html>`, o código PHP necessário para processar a validação e o envio dos dados é incluído.

O formulário HTML contém os campos `input` para capturar os dados necessários.

---

## 🇪🇸 Español (Original)

**Formulario simple en PHP con validación de campos y envío de datos.**

### Acerca del Formulario

Este proyecto proporciona un formulario que valida datos esenciales antes de su envío. Es ideal como base para proyectos que requieran captura de información del usuario.

El formulario valida los siguientes campos obligatorios:

- **Nombre**
- **Email**
- **Teléfono**

Gracias a HTML5, el campo **Email** aplica una validación automática de formato, mientras que **Nombre** y **Teléfono** son validados manualmente mediante PHP.

### Funcionamiento

Este formulario funciona con dos archivos principales:

- `class.phpmailer.php`
- `index.php`

En `index.php`, antes de la etiqueta `<html>`, se incluye el código PHP necesario para procesar la validación y el envío de los datos.

El formulario HTML contiene los `input` para capturar los datos requeridos.

---

## 🚀 Contribuciones

Gracias por considerar contribuir al proyecto. ¡Cualquier mejora, comentario o sugerencia es bienvenida!

## 📜 Código de Conducta

Para mantener una comunidad abierta y respetuosa, por favor revisa y respeta las buenas prácticas de contribución.

## 🔐 Vulnerabilidades de Seguridad

Si descubres alguna vulnerabilidad de seguridad en este formulario, por favor contáctame para poder solucionarlo de inmediato.

## 📄 Licencia

Este proyecto es software libre y está disponible bajo una licencia abierta. Puedes usarlo, modificarlo y adaptarlo libremente.

---

https://github.com/juanmafx
